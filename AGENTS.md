# OpsAI-IT 项目开发规则

## 1. 项目定位

- 本项目是面向企业内部员工和 IT 运维团队的 IT 运维知识库。
- 钉钉工作台是统一入口，提供免登身份和基础集成能力。
- 知识库由公司独立部署和管理。
- 第一期严格以 V1.1A 核心试点版为范围。

## 2. 文档优先级

开发前按以下顺序读取并遵循文档：

1. `docs/02-PRD/产品需求文档PRD-V1.1A-钉钉集成核心试点版-优化稿.md`；
2. `docs/04-Architecture/Django项目模块目录结构.md`；
3. `docs/04-Architecture/系统架构与API接口清单.md`；
4. `docs/05-Database/` 中的数据库模型文档与 `docs/07-ADR/` 中的架构决策；
5. `README.md` 和 `docs/06-Process/` 中的开发流程；
6. V1.1 PRD、调研资料和归档资料仅作历史参考。

如不同文档或现有实现发生冲突，不得擅自选择或静默折中，必须停止相关修改并报告冲突。

## 3. 技术栈约束

固定采用 Python 3.13、Django 5.2 LTS、PostgreSQL、Django Templates + Bootstrap + HTMX 和 Docker Compose，整体架构为模块化 Django 单体。

融合方向已获用户确认：以 OpsAI-IT 为主项目，Wagtail 作为依赖提供内容编辑、版本和审核基础能力；保留 accounts.User、组织关系、内容受众及各 App 职责，不维护 Wagtail 框架分叉。候选“Article 稳定业务身份 + 一对一 KnowledgeContent Snippet”仍待最小验证，详见 `docs/07-ADR/0002-Wagtail融合基线与最小设计.md`；不得据此删除 ArticleVersion、ReviewRecord 或维护两套可独立编辑的内容及发布状态。

未经明确批准，不得引入 React 或 Vue 前后端分离、微服务、Redis、Celery、Elasticsearch、Kubernetes 或独立 API 网关。

## 4. App 职责边界

- `core`：公共基类、统一异常、请求中间件、健康检查和无业务含义的通用能力。
- `accounts`：平台用户、部门、多部门关系、内容用户组、系统角色和账号状态。
- `knowledge`：知识空间、分类、文章、版本、受众、审核记录和附件元数据。
- `search`：查询规范化、服务端受众过滤、检索、排序、高亮和搜索日志。
- `workflow`：状态转换、事务边界、幂等、Outbox 和后台任务编排。
- `dingtalk`：免登、用户映射、通讯录同步、工作通知、回调和钉钉 API 适配。
- `service_desk`：安全上下文、字段映射、统一 IT 服务入口跳转及失败降级，不建设完整 ITSM。
- `audit`：敏感操作审计、请求上下文和变更前后快照，不承载业务状态机。

## 5. 代码组织规则

- View 只处理 HTTP 输入输出，状态变更放入 Service，复杂查询放入 Selector。
- 发布和审核等核心流程必须使用数据库事务，保证业务数据、审核记录、审计和 Outbox 一致。
- 核心业务流程不得依赖 Django Signal；Signal 仅可用于低风险附属动作。
- 搜索、详情和附件访问必须先在服务端执行内容受众过滤，不能由前端决定权限。
- 正式业务数据不得硬删除；文章使用下架或归档，用户使用禁用或离职状态，版本与审核记录永久保留。
- 不提前批量实现架构清单中的 91 个 API；一次只完成当前明确任务。

## 6. 安全规则

- 密钥、口令和第三方凭证只通过环境变量或受控密钥系统提供；`.env` 不得提交。
- 钉钉 AppSecret、访问令牌和授权码不得进入前端代码、普通日志或 Git。
- 附件必须私有存储，并通过鉴权下载；扫描通过前不得发布或下载。
- 内容受众规则遵循“拒绝优先”；无权限时不得泄露文章是否存在、标题、摘要或附件名。
- 日志不得记录令牌、密码、密钥或未脱敏的敏感信息。

## 7. 测试和验证规则

- 修改 Django 代码后运行 `python manage.py check`。
- 修改模型后必须生成并检查迁移，确认迁移内容与模型设计一致。
- 核心业务流程必须有自动化测试，覆盖权限、异常、重复请求和状态冲突。
- 提交前运行项目约定的格式检查、静态检查、相关测试和 `git diff --check`。
- 测试失败时不得声称完成；无法运行的检查必须说明原因、影响和待执行命令。

## 8. Git 规则

- 修改前先查看 `git status`，不得覆盖或夹带用户已有修改。
- 禁止使用 `git reset --hard`、`git clean -fd`、强制推送或其他破坏历史的操作。
- 不得提交 `.env`、虚拟环境、数据库文件、日志、密钥或无关生成物。
- 只有任务明确授权时才可创建本地提交；未经用户明确授权不得推送 GitHub 或 Gitee。
- 一个提交只包含一个明确阶段，提交前必须检查暂存内容和敏感信息。

### 8.1 提交说明规范

- 所有准备同步到 GitHub 和 Gitee 的新提交都必须使用“简明标题 + 详细正文”，不得只有一行标题。
- 提交标题应准确概括本阶段目标；提交正文必须包含“更新内容”“验证结果”“影响与回滚”三个部分。
- “更新内容”应说明本次新增、修改或删除的内容，以及涉及的主要文件或模块。
- “验证结果”应如实记录实际执行的格式检查、静态检查、自动化测试、Django 检查、迁移检查或健康检查；未执行或失败的项目必须明确说明，不得写成已通过。
- “影响与回滚”应说明是否影响业务逻辑、数据库、迁移、配置、部署或环境变量，并说明回滚该提交时的注意事项。
- 提交正文不得包含账号、密码、Token、`.env` 内容、个人代理地址或其他秘密信息。
- 不得为了补充说明而创建空提交；不得擅自 amend 已经推送的提交，也不得通过强制推送改写远端历史。

提交信息统一使用以下结构，并按实际变更填写：

```text
<type>: <本次提交的简明中文标题>

更新内容：
- <主要变更一>
- <主要变更二>

验证结果：
- <实际执行的检查及结果>

影响与回滚：
- <影响范围>
- <回滚方式或注意事项>
```

创建提交后、推送前必须使用以下命令检查完整提交信息：

```text
git show -s --format=full HEAD
```

如果一次准备推送多个新提交，必须逐个检查本地领先远端范围内的每个提交，确认每个提交都有完整且真实的更新说明。

### 8.2 GitHub + Gitee 双端安全推送记录

- 双端推送前必须确认 GitHub 与 Gitee 的目标分支基线一致，并确认本地提交相对两端均为纯快进；禁止 force push、修改远端地址或推送任务范围外的分支和标签。
- 双端推送后必须确认 GitHub、Gitee 与本地 `HEAD` 指向同一个完整提交哈希，检查两端领先/落后均为 `0 0`，并确认最终工作区状态干净。
- 每次双端推送的最终报告必须为本次推送范围内的每个提交列出：完整提交哈希、提交标题、主要更新、验证结果、影响范围和回滚提示。
- GitHub 与 Gitee 上同一提交的标题和正文必须完全一致，确保以后通过任一远端或本地 `git log` 都能理解该节点的更新内容并安全选择回滚点。
- 若一端推送成功、另一端失败，必须在报告中明确说明“部分成功”；不得重新创建提交、不得重复推送已成功的一端，只能在问题解决后重试失败的一端。
- 不需要每个小步骤都立即推送；双端推送按项目里程碑与用户明确授权执行。

## 9. Windows 开发环境规则

- 软件优先安装到 D 盘；项目 Python 虚拟环境固定放在仓库根目录 `.venv`。
- 不全局安装项目依赖，所有依赖安装和命令均使用项目虚拟环境。
- 含空格、中文或括号的路径必须完整引用，避免路径解析错误。
- 命令和操作说明应优先提供可在 Windows PowerShell 中直接执行的版本。

## AI 项目接力规则

本节用于约束 Agent 接管、任务推进、状态判断和交接，不替代第 2 节规定的需求与架构文档优先级。涉及产品范围、业务语义或架构约束时，仍以 V1.1A PRD 和对应架构决策为准；发现冲突必须停止相关修改并报告，不得借接力之名修改 PRD、静默改变业务语义或扩大任务范围。

### 1. 新 Agent 开工必读顺序

新 Agent 开始任何任务前，应按以下顺序读取：

1. `AGENTS.md`；
2. `docs/PROJECT.md`；
3. `docs/PROJECT_STATE.md`；
4. `docs/HANDOFF.md`；
5. `tasks/TASKS.yaml`；
6. `tasks/CURRENT_TASK.md`。

上述文件存在时必须读取；尚未建立时，不得因此自行创建，也不得跳过项目负责人安排的阶段任务。开始修改前还必须核对实际仓库路径、当前分支、`HEAD` 和工作区状态，保护用户已有修改。

### 2. 项目真实状态证据优先级

判断“项目现在是什么状态、某任务是否已经完成”时，按以下证据优先级核对：

1. 实际代码和文件；
2. 实际执行的自动测试与验证结果；
3. Git 历史；
4. `tasks/TASKS.yaml`；
5. `docs/PROJECT_STATE.md`；
6. `docs/HANDOFF.md`；
7. 历史聊天。

聊天记录不能单独作为项目真实状态依据。测试报告、提交说明和状态文档必须能回溯到实际文件、Git 或可复核的验证结果；证据互相冲突时必须记录并报告，不得选择最方便的说法。该优先级只用于判断现场状态，不得用来绕过第 2 节规定的需求与架构文档优先级。

### 3. 单任务原则

- Agent 每轮原则上只处理一个明确任务。
- `tasks/CURRENT_TASK.md` 存在后，只能处理其中定义的当前任务。
- `tasks/CURRENT_TASK.md` 尚未建立时，只能执行项目负责人明确授权的任务。
- 没有唯一当前任务时，不得自行选择或开始 `READY` 任务，也不得自动进入下一阶段。

### 4. 禁止扩大任务范围

发现任务外的问题时，按“记录 → 汇报 → 等待项目负责人决定”处理。禁止顺手修复、顺手重构、顺手升级依赖、顺手修改其他模块或提前完成后续阶段；未经授权不得修改 PRD、数据库、部署配置或其他任务文件。

### 5. 禁止重复实现

新 Agent 开工前必须检查 Git、实际代码、相关测试，以及存在的 `docs/PROJECT_STATE.md`、`tasks/TASKS.yaml` 和 `docs/HANDOFF.md`。已有功能或任务存在充分证据时不得重新实现；证据不足或描述不一致时，应先报告缺口并等待确认，不得用重复开发替代核验。

### 6. DONE 条件

“代码写完”不等于任务 `DONE`。任务至少应满足：

- 验收条件已完成；
- 与风险相称的测试或验证已实际执行并如实记录；
- Git 分支、`HEAD`、工作区和任务范围内差异明确；
- 任务要求的状态文档已更新；尚未建立或任务未授权创建的状态文件不得提前创建；
- 任务要求且已获授权时，已形成范围单一、说明完整的 Commit；
- 任务要求且已获授权 Push 时，已完成远端核验。

任何必要检查未执行、失败或证据不足时，不得标记 `DONE`，必须说明原因、影响和下一步。涉及 GitHub 与 Gitee 双端同步时，只有本地 `HEAD`、GitHub 目标分支 HEAD、Gitee 目标分支 HEAD 实际核验一致，且领先/落后状态符合任务要求后，才能声明“双端同步完成”。

### 7. Agent 更换与 HANDOFF 条件

出现以下任一情况时，应进入交接流程：Codex 额度耗尽；主动切换 Qoder、Claude 或其他 Agent；更换电脑；会话即将结束且任务未完成；当前 Agent 无法继续；遇到阻塞需要他人接手。

交接不得只写“做到一半”，必须记录：当前任务、已完成内容、未完成内容、修改文件、Git 分支、完整 `HEAD`、相关 Commit、测试或验证结果、阻塞点、下一步和风险。不得伪造不存在的执行历史、测试结果、Commit、Push 或交接记录。

### 8. PROJECT_STATE 更新规则

`docs/PROJECT_STATE.md` 建立后，Agent 必须确保其中的已完成、当前进行中、尚未正式接入、阻塞、技术债、测试证据、Git 状态和下一步与真实项目状态一致。状态结论必须有代码、文件、Git 或验证证据支持；文件尚未建立或当前任务未授权时不得自行创建。

### 9. TASKS.yaml 更新规则

`tasks/TASKS.yaml` 建立后，任务状态必须由实际证据驱动。不得因为历史聊天声称完成就直接标记 `DONE`；至少应核对对应代码或文件、Git 记录及任务要求的测试或验证结果。文件尚未建立或当前任务未授权时不得自行创建。

### 10. CURRENT_TASK 规则

`tasks/CURRENT_TASK.md` 建立后，Agent 只能处理其中定义的唯一当前任务。若文件中没有唯一任务、内容与负责人指令冲突或任务前置条件不成立，必须停止扩展实施并报告；不得自行开始其他 `READY` 任务。文件尚未建立或当前任务未授权时不得自行创建。

### 11. HANDOFF 更新规则

`docs/HANDOFF.md` 建立后，Agent 切换、任务中断或出现本节第 7 条所列情况时必须按实际现场更新。尚未建立时，应使用项目负责人明确指定且任务范围允许的临时交接载体；不得自行提前创建，也不得把临时报告伪装成正式历史交接。

### 12. 接力中的敏感信息保护

`.env`、`SECRET_KEY`、数据库密码、API Key、Token、Cookie、私钥及其他凭证不得写入 Git、项目文档、日志、交接报告或聊天输出。接力记录最多说明变量名、是否必需、是否存在、是否被 Git 跟踪或忽略以及安全配置方式，不得读取、复制或输出真实值。

### 13. PoC 与正式能力边界

新 Agent 不得把实验目录、PoC 或自动测试成功描述成正式上线能力。当前 `experiments/wagtail_*` 属于 Wagtail 隔离融合实验；其中的代码、SQLite 内存测试、实验 URL 或历史验证结果不自动等同于正式业务入口、PostgreSQL 验证、生产部署或生产可用。能力成熟度必须按实际接入路径和已完成的环境验证如实说明。

## 10. 当前项目状态

- V1.1A 文档开发基线已经建立。
- Django 单体项目骨架已初始化，`/health/live` 与 `/health/ready` 可运行并有自动化测试。
- 电脑 B 的 Docker Desktop 4.92.0、WSL 2、Docker Engine 29.8.0 和 Docker Compose v5.5.1 已就绪；`deploy/compose.yaml` 已定义 db + web 服务，但当前电脑 B 尚未创建或启动 OpsAI PostgreSQL 容器、命名卷或数据库。
- 正式开发数据库目标仍为 PostgreSQL（Compose 内 `POSTGRES_HOST` 覆盖为 `db`）；电脑 B 的真实 `.env` 尚不存在，未执行 `migrate`，测试环境继续使用 SQLite 内存库。
- 里程碑：第一阶段第 1～6 步已完成，里程碑 B 的代码、数据模型、迁移和测试底座已经建立；第 7 步（本地模拟登录与系统操作角色）已经完成，安全收紧提交 c5f1bbb 已通过第 7C 只读审计（结论 A：完整通过），PostgreSQL 补充只读核验已经完成。
- accounts：`User`、`Department`、`UserDepartment`、`UserGroup`、`UserGroupMembership` 已正式建立，`0001`、`0002` 已在 PostgreSQL 应用，相关约束和模型测试已通过；`UserGroup` 仅用于内容受众，系统操作角色仍使用 Django `Group`/`Permission`。
- knowledge：`KnowledgeSpace`、`Category`、`Article`、`ArticleVersion`、`ArticleAudience`、`ReviewRecord` 已接入，`0001`、`0002` 已在 PostgreSQL 应用；核心外键、唯一约束、CHECK、条件唯一索引和模型校验已经建立；当前未实现发布事务、编号生成服务、搜索、API、页面或钉钉功能；内容受众 Selector 已实现。
- Article 编号：数据库只允许 `KB-000001` 格式的知识编号；编号生成服务尚未实现，在其安全接入前 Article Admin 新增入口保持关闭；不得通过 Admin、Signal、随机默认值或临时拼接绕过编号规则。
- 候选参考：`docs/05-Database/knowledge_models_reference.py` 继续作为设计参考保留；Django 运行时正式模型位于 `apps/knowledge/models.py`，后续开发不得重新复制候选文件覆盖正式模型。
- 第 8D 步隔离验证：全量 pytest 379 passed、11 subtests passed；Django check、迁移一致性、Ruff 静态与格式检查通过。仅使用 SQLite 内存测试库；未读取真实 .env，未核验 PostgreSQL、容器、持久库数据或数据库注释。
- 第 8D 步已修复 IT 组配置边界、未保存用户门槛和 base 重复行，并加入受众 Inline 最终状态校验；系统角色权限不变。新增 knowledge.0003 仅修改空间默认策略 db_comment，未应用到持久数据库。
- 系统操作角色权限矩阵（0/8/6/19）：普通员工 0 项、知识编辑员 8 项、知识审核员 6 项、知识库管理员 19 项；均不含 delete 权限，已移除 accounts.change_user、auth.change_group、knowledge.change_reviewrecord 等危险权限。
- 本地模拟登录：仅限 DEBUG=True 且显式开启开发开关（DJANGO_DEV_LOGIN_ENABLED，默认关闭，仅识别 1/true/yes/on）；production.py 硬编码强制关闭；固定开发身份为 dev_employee、dev_editor、dev_reviewer、dev_knowledge_admin（均 is_superuser=False、密码不可用、仅属各自系统角色，仅 dev_knowledge_admin 为 staff=True）。
- Article 编号服务尚未实现，Article Admin 新增入口仍保持关闭；第 8 步内容受众 Selector 已实现（`apps/knowledge/selectors.py` 的 `visible_articles` / `can_read_article`），尚未接入页面、HTTP API、搜索或附件。
- 内容受众语义（第 8B 步，见 `docs/07-ADR/0001`）：账号门槛 → 文章策略（`all_employees` / `it_only` / `restricted`）→ 拒绝优先；员工阅读路径不因 `is_staff` / `is_superuser` / 编辑员 / 审核员 / 知识管理员 / 作者 / 空间负责人越权；`it_only` 通过服务端配置 `KNOWLEDGE_IT_USER_GROUP_ID` 绑定一个启用的内容用户组，未配置时不授予 it_only 阅读权限；读取侧按文章策略计算允许范围，不一致 allow 不扩大范围。
- 第 8F 步已实现：常规联合编辑、非循环更新链及授权删除／修改／新增混合操作；循环交换首版不支持，在表单阶段中文提示、保留输入、整次不保存。不删除重插既有规则，不改变数据库约束或角色权限，无新增迁移。
- 以下成绩引用用户提供的此前完成报告，不是第 8H 文档同步时重新执行的结果。第 8F 历史验证：隔离专项 159 passed；完整套件 386 passed、11 subtests passed；Django check、迁移一致性及 Ruff 检查通过。
- 第 8G 定点复核：A，按调整后的范围通过。项目专项 76 passed、仓库外补充复现 12 passed；Django check、迁移一致性、Ruff 静态与格式、工作区及提交差异空白检查通过；第 8G 未重跑完整套件。第 8F 确定性保存缺陷已按当前契约关闭；PostgreSQL 及持久库迁移仍需另行安排。
- 仓库迁移文件：accounts.0001～0002、knowledge.0001～0003 已存在。上文 PostgreSQL 已应用 0001、0002 及 knowledge.0003 未应用的说明属于此前报告；当前持久数据库迁移状态、实际数据库注释仍待核验，不从文件存在推断数据库已更新。
- PostgreSQL 查询、排序及相关数据库行为待验证。未新增并发协调锁，实际并发及锁行为未验证；并发写入仍可能冲突或覆盖更新，atomic() 不等同于并发安全。
- 当前阶段：F02～F08 已形成 Wagtail 隔离 PoC 证据，但 Wagtail 仍未进入正式根依赖、正式 settings 或正式路由；不得把实验测试、SQLite 结果或历史成绩写成正式接入、PostgreSQL 或生产验证通过。
- 第一阶段 AI 接力文件 H01～H08 已建立并完成双端同步；OPS-H09 仅检查和修正接力元数据一致性。当前唯一接力业务任务是任务 9，状态为 `READY`、负责人为 `unassigned`，尚未授权业务实施或指定执行 Agent。
- 任务 8J 因电脑 B 缺少真实 `.env` 且没有数据库操作授权保持 `BLOCKED`；该阻塞只影响 PostgreSQL 持久环境补充核验，不自动阻塞任务 9 或整个项目。
- OPS-H09 完成后停止，不自动执行 OPS-H10、任务 9、数据库操作、Commit 或 Push；后续任务按用户明确授权推进。同步状态以对 GitHub、Gitee 的实时核验为准。
- 规则文档不得写入密码、个人代理或机器专属临时路径。
