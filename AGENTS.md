# OpsAI-IT 项目开发规则

## 1. 项目定位

- 本项目是面向企业内部员工和 IT 运维团队的 IT 运维知识库。
- 钉钉工作台是统一入口，提供免登身份和基础集成能力。
- 知识库由公司独立部署和管理。
- 第一期严格以 V1.1A 核心试点版为范围。

## 2. 文档优先级

开发前按以下顺序读取并遵循文档：

1. `docs/02-PRD/产品需求文档PRD-V1.1A-钉钉集成核心试点版-优化稿.md`；
2. `docs/04-Architecture/Django项目模块目录结构.md`；
3. `docs/04-Architecture/系统架构与API接口清单.md`；
4. `docs/05-Database/` 中的数据库模型文档与 `docs/07-ADR/` 中的架构决策；
5. `README.md` 和 `docs/06-Process/` 中的开发流程；
6. V1.1 PRD、调研资料和归档资料仅作历史参考。

如不同文档或现有实现发生冲突，不得擅自选择或静默折中，必须停止相关修改并报告冲突。

## 3. 技术栈约束

固定采用 Python 3.13、Django 5.2 LTS、PostgreSQL、Django Templates + Bootstrap + HTMX 和 Docker Compose，整体架构为模块化 Django 单体。

未经明确批准，不得引入 React 或 Vue 前后端分离、微服务、Redis、Celery、Elasticsearch、Kubernetes 或独立 API 网关。

## 4. App 职责边界

- `core`：公共基类、统一异常、请求中间件、健康检查和无业务含义的通用能力。
- `accounts`：平台用户、部门、多部门关系、内容用户组、系统角色和账号状态。
- `knowledge`：知识空间、分类、文章、版本、受众、审核记录和附件元数据。
- `search`：查询规范化、服务端受众过滤、检索、排序、高亮和搜索日志。
- `workflow`：状态转换、事务边界、幂等、Outbox 和后台任务编排。
- `dingtalk`：免登、用户映射、通讯录同步、工作通知、回调和钉钉 API 适配。
- `service_desk`：安全上下文、字段映射、统一 IT 服务入口跳转及失败降级，不建设完整 ITSM。
- `audit`：敏感操作审计、请求上下文和变更前后快照，不承载业务状态机。

## 5. 代码组织规则

- View 只处理 HTTP 输入输出，状态变更放入 Service，复杂查询放入 Selector。
- 发布和审核等核心流程必须使用数据库事务，保证业务数据、审核记录、审计和 Outbox 一致。
- 核心业务流程不得依赖 Django Signal；Signal 仅可用于低风险附属动作。
- 搜索、详情和附件访问必须先在服务端执行内容受众过滤，不能由前端决定权限。
- 正式业务数据不得硬删除；文章使用下架或归档，用户使用禁用或离职状态，版本与审核记录永久保留。
- 不提前批量实现架构清单中的 91 个 API；一次只完成当前明确任务。

## 6. 安全规则

- 密钥、口令和第三方凭证只通过环境变量或受控密钥系统提供；`.env` 不得提交。
- 钉钉 AppSecret、访问令牌和授权码不得进入前端代码、普通日志或 Git。
- 附件必须私有存储，并通过鉴权下载；扫描通过前不得发布或下载。
- 内容受众规则遵循“拒绝优先”；无权限时不得泄露文章是否存在、标题、摘要或附件名。
- 日志不得记录令牌、密码、密钥或未脱敏的敏感信息。

## 7. 测试和验证规则

- 修改 Django 代码后运行 `python manage.py check`。
- 修改模型后必须生成并检查迁移，确认迁移内容与模型设计一致。
- 核心业务流程必须有自动化测试，覆盖权限、异常、重复请求和状态冲突。
- 提交前运行项目约定的格式检查、静态检查、相关测试和 `git diff --check`。
- 测试失败时不得声称完成；无法运行的检查必须说明原因、影响和待执行命令。

## 8. Git 规则

- 修改前先查看 `git status`，不得覆盖或夹带用户已有修改。
- 禁止使用 `git reset --hard`、`git clean -fd`、强制推送或其他破坏历史的操作。
- 不得提交 `.env`、虚拟环境、数据库文件、日志、密钥或无关生成物。
- 只有任务明确授权时才可创建本地提交；未经用户明确授权不得推送 GitHub 或 Gitee。
- 一个提交只包含一个明确阶段，提交前必须检查暂存内容和敏感信息。

## 9. Windows 开发环境规则

- 软件优先安装到 D 盘；项目 Python 虚拟环境固定放在仓库根目录 `.venv`。
- 不全局安装项目依赖，所有依赖安装和命令均使用项目虚拟环境。
- 含空格、中文或括号的路径必须完整引用，避免路径解析错误。
- 命令和操作说明应优先提供可在 Windows PowerShell 中直接执行的版本。

## 10. 当前项目状态

- V1.1A 文档开发基线已经建立。
- Django 单体项目骨架已初始化，`/health/live` 与 `/health/ready` 可运行并有自动化测试。
- Docker Desktop 4.89 与 WSL 2 已就绪；PostgreSQL 本地开发环境已通过 `deploy/compose.yaml`（db + web 服务）建立，数据位于 Docker 的 WSL 2 数据盘。
- 本地开发数据库使用 PostgreSQL（连接变量由未跟踪的 `.env` 提供，`POSTGRES_HOST` 在 Compose 内覆盖为 `db`）；测试环境继续使用 SQLite 内存库。
- `docs/05-Database/knowledge_models_reference.py` 只是候选模型参考代码，当前不会被 Django 加载。
- 下一步是按 Codex 分阶段开发指令第 6 步接入 `knowledge` 核心数据模型；禁止提前实现业务功能。
- 规则文档不得写入密码、个人代理或机器专属临时路径。
